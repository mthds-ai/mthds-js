# Roadmap

## Planned

- **Stale package cleanup** — detect packages whose slug no longer exists in the source repo and hide or remove them from the hub
- **CLI API runner config** — allow `mthds run` to target a self-hosted Pipelex API instance via CLI flags (e.g. `--api-url`, `--api-key`) instead of requiring the SDK
